# ── Copy this file to .env and fill in your values ───────────────────────────
# cp .env.example .env

# Required — get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# ── Slack (add after MVP) ─────────────────────────────────────────────────────
# Create an Incoming Webhook at https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# ── Email / SMTP (add after MVP) ──────────────────────────────────────────────
# Example values below are for Gmail with an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=you@gmail.com
EMAIL_TO=recipient@example.com

# ── Reddit (add after MVP) ────────────────────────────────────────────────────
# Register an app at https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=cybersec-agent/1.0

# ── Agent behaviour (optional — defaults shown) ───────────────────────────────
# Poll interval in seconds (300 = 5 minutes)
POLL_INTERVAL_SECONDS=300
# Minimum severity score (1-10) to trigger a notification
SEVERITY_THRESHOLD=6
# Path to the SQLite deduplication database
DB_PATH=data/seen_items.db
